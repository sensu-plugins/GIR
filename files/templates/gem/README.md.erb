## Sensu-Plugins-<%= @plugin_name %>

[![Build Status](https://travis-ci.org/sensu-plugins/sensu-plugins-<%= @plugin_name %>.svg?branch=master)](https://travis-ci.org/sensu-plugins/sensu-plugins-<%= @plugin_name %>)
[![Gem Version](https://badge.fury.io/rb/sensu-plugins-<%= @plugin_name %>.svg)](http://badge.fury.io/rb/sensu-plugins-<%= @plugin_name %>)
[![Code Climate](https://codeclimate.com/github/sensu-plugins/sensu-plugins-<%= @plugin_name %>/badges/gpa.svg)](https://codeclimate.com/github/sensu-plugins/sensu-plugins-<%= @plugin_name %>)
[![Test Coverage](https://codeclimate.com/github/sensu-plugins/sensu-plugins-<%= @plugin_name %>/badges/coverage.svg)](https://codeclimate.com/github/sensu-plugins/sensu-plugins-<%= @plugin_name %>)
[![Dependency Status](https://gemnasium.com/sensu-plugins/sensu-plugins-<%= @plugin_name %>.svg)](https://gemnasium.com/sensu-plugins/sensu-plugins-<%= @plugin_name %>)

## Functionality

## Files
 *
 *
 *
 *

## Usage

## Installation

Add the public key (if you haven’t already) as a trusted certificate

```
gem cert --add <(curl -Ls <%= PUBLIC_KEY %>)
gem install <%= @gem_root %> -P MediumSecurity
```

You can also download the key from /certs/ within each repository.

#### Rubygems

`gem install <%= @gem_root %>`

#### Bundler

Add *sensu-plugins-<%= @gem_root %>* to your Gemfile and run `bundle install` or `bundle update`

#### Chef

Using the Sensu **sensu_gem** LWRP
```
sensu_gem '<%= @gem_root %>' do
  options('--prerelease')
  version '0.0.1'
end
```

Using the Chef **gem_package** resource
```
gem_package '<%= @gem_root %>' do
  options('--prerelease')
  version '0.0.1'
end
```

## Notes
